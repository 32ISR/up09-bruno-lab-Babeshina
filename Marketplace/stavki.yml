info:
  name: stavki
  type: http
  seq: 7

http:
  method: GET
  url: https://kitek.ktkv.dev/marketplace/api/items/:id/bids
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
