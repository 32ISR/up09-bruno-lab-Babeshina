info:
  name: create
  type: http
  seq: 5

http:
  method: POST
  url: |-
    {
      "username": "testuser",
      "password": "password123"
    https://kitek.ktkv.dev/marketplace/api/items
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
